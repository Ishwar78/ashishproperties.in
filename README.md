Ashish Properties Modern real-estate web app for listing, discovery, and lead generation. Built with React + Vite (client) and Express + TypeScript + MongoDB (server). Supports categories/subcategories, property CRUD, chat/enquiries, admin moderation, banners/ads, SEO-friendly pages, Razorpay & PhonePe payments, notifications, and a PWA installable experience. Live: https://ashishproperties.in

<img width="1906" height="904" alt="image" src="https://github.com/user-attachments/assets/5a941a4e-75f8-4c72-bf68-a109a61609d6" />
⚙️ Tech Stack Frontend: React 18, Vite, TypeScript, TailwindCSS, shadcn/ui, React Router State/Forms/UX: react-hook-form, sonner/toast, dnd-kit, recharts Backend: Node.js, Express, TypeScript, MongoDB Atlas (native driver) Payments: Razorpay, PhonePe (controller + routes) Auth & Roles: JWT middleware (buyer, seller/agent, admin) Notifications & Chat: Socket.io chat scaffolding, unread counts, email/OTP flows PWA: manifest.webmanifest, install banner, offline-friendly routes DevOps: Netlify (client), PM2/Nginx (server), environment-based builds 🧭 Features Property Browsing Categories & subcategories (Residential, Commercial, Plots, etc.) Filters (price, location, type…), detail pages, image gallery/zoom “Call/WhatsApp” CTAs, enquiry modal, share buttons Posting & Management Post property (seller/agent), draft → review → publish workflow Admin moderation (approve/reject/restore/permanent-delete) Packages & ad slots (banners, homepage positions) Accounts
<img width="1910" height="980" alt="image" src="https://github.com/user-attachments/assets/cc29c33d-be67-411f-b72d-bc5b59fe719a" />
Comprehensive auth (email/OTP), role-based screens (Buyer/Seller/Agent/Admin) Buyer dashboard, Agent/Seller dashboard, Admin panel Payments Razorpay orders & verification PhonePe controller & webhooks for reconciliation Comms In-app chat + conversation list, unread counts Notifications (subscribe/unread), email verification helpers PWA & SEO Installable app (mobile & desktop) Meta tags, clean routes, Netlify SPA redirects
<img width="1885" height="897" alt="image" src="https://github.com/user-attachments/assets/38a9f2cd-1d2a-41fe-b2ee-347cdb3dca81" />
🗂 Project Structure (high level) / ├─ client/ # React (Vite) app │ ├─ pages/ # 80+ screens (Buy, Sell, Admin*, Chat, Checkout…) │ ├─ components/ # Header, Footer, Filters, Admin tables, Modals… │ ├─ contexts/ hooks/ lib/ utils/ │ ├─ global.css index.html main.tsx App.tsx │ ├─ .env.development .env.production │ └─ public/ (static assets) │ ├─ server/ # Express + TypeScript │ ├─ index.ts # main app (routes, middleware, sockets) │ ├─ db/mongodb.ts # MongoDB connection (Atlas) │ ├─ middleware/auth.ts # JWT, role guards (admin/seller/buyer) │ ├─ routes/ # auth, properties, categories, payments, chat… │ │ ├─ properties.ts │ │ ├─ categories.ts / categories-new.ts │ │ ├─ admin*.ts (moderation, settings) │ │ ├─ phonepe*.ts / razorpay-payments.ts │ │ ├─ notifications*.ts, chat*.ts, reports.ts, packages.ts, etc. │ ├─ controllers/phonepe-payments.ts │ ├─ socketio.ts websocket.ts │ └─ utils/ config/ scripts/ │ ├─ manifest.webmanifest # PWA ├─ netlify.toml # SPA redirects for client ├─ ecosystem.config.cjs # PM2 server config (production) ├─ package.json # root scripts (build:client/server, start) ├─ nginx-config-hostinger.conf (example) └─ README.md # (this file) 🔐 Environment Variables Never commit real credentials. Use .env files locally / provider secrets in production. Server (Express) # Core NODE_ENV=development PORT=8017 MONGODB_URI=mongodb+srv://:@/?retryWrites=true&w=majority DB_NAME=aashish_property JWT_SECRET=your-strong-secret # Razorpay RAZORPAY_KEY_ID=xxx RAZORPAY_KEY_SECRET=xxx # PhonePe PHONEPE_MERCHANT_ID=xxx PHONEPE_SALT_KEY=xxx PHONEPE_SALT_INDEX=1 PHONEPE_BASE_URL=https://api.phonepe.com/apis/hermes # or sandbox # CORS / APP CLIENT_ORIGIN=https://ashishproperties.in
<img width="1887" height="963" alt="image" src="https://github.com/user-attachments/assets/9be3a9c4-255a-448e-8bfa-559fb89a4ca2" />
Client (Vite) VITE_API_BASE_URL=https://api.ashishproperties.in # your server base URL VITE_RAZORPAY_KEY_ID=xxx VITE_PHONEPE_ENV=sandbox | production 🧑‍💻 Local Development Prereqs Node.js 18+ MongoDB Atlas (or local MongoDB) Yarn/NPM/Pnpm (any) 1) Install deps npm install 2) Set envs Create server vars in a root .env or process manager (PM2) env. Create client vars in client/.env.development. 3) Run dev Client (Vite): npm run dev # starts Vite on default port Server (Express): Use tsx/ts-node or a small script (e.g., server/start-server.ts) to run with your env: npx tsx server/start-server.ts # or compile & run if you prefer 🏗 Build & Production Build # root package.json npm run build # runs build:client + build:server npm run build:client # Vite client build → client/dist npm run build:server # Server build → dist/server Run npm start # node dist/server/index.mjs Netlify (Client) Uses netlify.toml: command = "npm run build:client" publish = "client/dist" SPA fallback /* → /index.html PM2 (Server) ecosystem.config.cjs example already present. Common flow: pm2 start ecosystem.config.cjs pm2 logs pm2 save Nginx Reverse Proxy Use the sample nginx-config-hostinger.conf as a reference for proxying api.ashishproperties.in → Node port. 🔌 API Overview (selected) Base: /api Auth: POST /auth/login, POST /auth/register, GET /auth/me Properties: GET /properties, POST /properties (seller/agent), PATCH /properties/:id (admin), soft-delete/restore/permanent-delete Categories: GET /categories, GET /categories/:id, admin CRUD for categories/subcategories Chat: GET /chat/conversations, GET /chat/:id/messages, POST /chat/send Notifications: GET /notifications/unread, POST /notifications/subscribe Payments: Razorpay: POST /razorpay/create-order, POST /razorpay/verify PhonePe: POST /phonepe/initiate, POST /phonepe/webhook (controller handles signature verify) Admin: /admin/* (moderation, settings, banners, packages) Many more routes exist (blog, reports, maps, other services) — see server/routes/. 🧪 Testing npm test # vitest --run 📦 Scripts (root) { "dev": "node node_modules/vite/bin/vite.js", "build": "npm run build:client && npm run build:server", "build:client": "node node_modules/vite/bin/vite.js build", "build:server": "node node_modules/vite/bin/vite.js build --config vite.config.server.ts", "start": "node dist/server/index.mjs", "test": "vitest --run", "test:db": "cd server && npx tsx scripts/testConnection.ts", "format.fix": "prettier --write .", "typecheck": "tsc" } 🔒 Security & Data Never commit real DB or payment secrets. Rotate API keys regularly. Protect main branch and restrict force-push after your LFS cleanup. Validate uploads and sanitize user input on all write routes. 🚀 Roadmap Ideas Advanced search (geo radius, map pins, saved searches) Image CDN + server-side image processing SMS/WhatsApp lead alerts Property comparison & shortlists CI/CD pipeline (build & deploy hooks) E2E tests (Playwright/Cypress) 👥 Author / Company Ashish Properties — engineered by Satya Web Technology Office No. 06, Opp. Oxygen Hospital, Near Durga Bhawan Mandir, Rohtak — 124001 Phone: 93184 17190 📄 License Proprietary — All rights reserved by Ashish Properties / Satya Web Technology (internal/commercial use only).
